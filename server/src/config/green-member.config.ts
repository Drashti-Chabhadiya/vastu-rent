export const greenMemberConfig = {
  /**
   * The minimum number of listings (products) a user must have to be eligible.
   */
  minListings: 1,

  /**
   * The minimum number of rentals/bookings a user must have.
   */
  minRentals: 0,

  /**
   * Whether identity verification (emailVerified) is required.
   */
  requireVerified: true,

  /**
   * Whether a phone number must be added to their profile.
   */
  requirePhone: true,

  /**
   * Whether location details must be filled in.
   */
  requireLocation: true,
};
